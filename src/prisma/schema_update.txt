// Adicione estas linhas ao model Character:

model Character {
  // ... campos existentes ...

  // NOVOS CAMPOS PARA YEAR ZERO
  stress_level     Int?    @default(0)
  trauma_level     Int?    @default(0)
  willpower        Int?    @default(0)
  experience       Int?    @default(0)
  
  // ... resto dos campos ...
}
